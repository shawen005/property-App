<%= form_with(model: listing_image, local: true) do |form| %>
  <% if listing_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(listing_image.errors.count, "error") %> prohibited this listing_image from being saved:</h2>

      <ul>
      <% listing_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.number_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_field :img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
